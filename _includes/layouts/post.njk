---
layout: layouts/base.njk
---
{% macro prevNextLink(post, type) %}
<a
  href="{{ post.url | url }}"
  rel="{{ type }}"
>
  {{ post.data.title }}
</a>
{% endmacro %}

{%- set nextPost = collections.posts | getNextCollectionItem(page) %}
{%- set previousPost = collections.posts | getPreviousCollectionItem(page) %}

<head>
  <link rel="stylesheet" href="/css/highlight.css">
  <link rel="stylesheet" href="/css/littlefoot.css">
</head>

<article>
  <h1 style="margin-bottom: 30px;">{{ title }}</h1>
  {{ content | safe }}
</article>

<div style="padding-top: 10px; border-top: 1px dashed;">
  {%- if previousPost %}
    Previous: {{ prevNextLink(previousPost, 'previous') }}
  {% endif %}
  <br />
  {%- if nextPost %}
    Next: {{ prevNextLink(nextPost, 'next') }}
  {% endif %}
</div>
